.PHONY: setup build test run clean

setup:
	chmod +x ./gradlew
	./gradlew clean install

build:
	chmod +x ./gradlew
	./gradlew build

test:
	chmod +x ./gradlew
	./gradlew test

run:
	chmod +x ./gradlew
	./gradlew run

clean:
	chmod +x ./gradlew
	./gradlew clean
